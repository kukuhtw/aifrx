POSTGRES_PASSWORD=change-me
DATABASE_URL=postgres://aiforex:change-me@postgres:5432/aiforex
TELEGRAM_BOT_TOKEN=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
MT5_BRIDGE_URL=http://mt5-bridge:8000
MT5_BRIDGE_ROUTES={}
MT5_BRIDGE_API_KEY=replace-with-long-random-secret
# Generate: openssl rand -base64 32
ENCRYPTION_KEY=
TRADING_MODE=DEMO
LIVE_TRADING_ENABLED=false
MARKET_DATA_MAX_AGE_SECONDS=30
RUST_LOG=info
MT5_MODE=MOCK
# Admin dashboard accounts are not configured through env vars. After the
# backend is running once (migrations applied), bootstrap the first OWNER
# admin with: cargo run -p ai-forex-backend --bin create-admin
# See docs/admin-dashboard.md.
